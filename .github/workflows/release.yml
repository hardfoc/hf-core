---
name: 📦 Release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  release:
    name: Create Release with Artifacts
    uses: N3b3x/hf-espidf-ci-tools/.github/workflows/ru-release-with-artifacts.yml@main
    with:
      build_workflow: 'esp32-examples-build-ci.yml'
      artifact_pattern: 'fw-.*'
      fail_on_no_artifacts: false
